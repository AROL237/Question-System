spring:
  application:
    name: customer-service
  datasource:
    username: root
    password: Nzonkem@237
    url: jdbc:mysql://localhost:3306/customer_service
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
  profiles:
    active: customer
server:
  port: 8081
my:
  app:
    secret: "arollito123"
    token_duration: 60
