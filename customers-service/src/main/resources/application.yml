spring:
  application:
    name: customer-service
  datasource:
    username: root
    password: Nzonkem@237
    url: jdbc:mysql://localhost:3306/customer_service
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
server:
  port: 8081
